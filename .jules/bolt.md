## 2024-11-28 - Optimizing GameList.tsx renders\n**Learning:** The React render loop inside `GameList.tsx` was recalculating `hasActiveTitle` for every mapped game due to `Object.values(runningStatus).some(Boolean)` being inside the `.map` function, causing O(N^2) complexity.\n**Action:** Pulled the calculation of `hasActiveTitle` out of the loop since it evaluates the same value for all games.

## 2024-11-28 - GitHub Actions CI Conventional Commits\n**Learning:** The repository enforces Conventional Commits on PR titles using `amannn/action-semantic-pull-request`. My PR title `perf: ⚡ Bolt: [performance improvement] Optimize GameList.tsx render loop` failed because the tool doesn't always parse emojis or specific prefixes correctly when mixed. The title must strictly start with the allowed prefix (like `perf:`).\n**Action:** Always start the PR title exactly with the prefix (e.g. `perf:`) without emojis before it.
