--- src/renderer/src/components/game-list/GameRow.tsx
+++ src/renderer/src/components/game-list/GameRow.tsx
@@ -677,9 +677,13 @@
     prev.isLaunching !== next.isLaunching ||
     prev.isLaunchBlocked !== next.isLaunchBlocked ||
     prev.cacheInitialized !== next.cacheInitialized ||
-    prev.game.key !== next.game.key ||
-    prev.game.name !== next.game.name
+    prev.game !== next.game ||
+    prev.onLaunchStart !== next.onLaunchStart ||
+    prev.onLaunchEnd !== next.onLaunchEnd ||
+    prev.onRunningStateRefresh !== next.onRunningStateRefresh ||
+    prev.onToggleEditor !== next.onToggleEditor ||
+    prev.onCloseEditor !== next.onCloseEditor
   ) {
     return false
   }
   if (prev.runningAppIcons.length !== next.runningAppIcons.length) return false
